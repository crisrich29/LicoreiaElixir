services:
  - type: static_site
    name: licoreria-elixir
    env: static
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    root: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
